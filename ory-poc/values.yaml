httpbin:
  namespace: httpbin

helloworld:
  namespace: hello-world

host: kyma-host

global:
  ingress:
    domainName: bc-ir.goatz.shoot.canary.k8s-hana.ondemand.com
  istio:
    gateway:
      namespace: kyma-system
      name: kyma-gateway
  oathkeeper:
    host: ory-oathkeeper-proxy.kyma-system.svc.cluster.local
    port: 4455
